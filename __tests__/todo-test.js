import 'react-native';

import React from 'react';
import { render, fireEvent } from '@testing-library/react-native';

import Todo from '../src/screens/userMenu';

jest.mock('');

afterEach(() => {
  jest.clearAllMocks();
});

test('test in progress', () => {});
